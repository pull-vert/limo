sourceCompatibility = JavaVersion.VERSION_14
targetCompatibility = JavaVersion.VERSION_14

dependencies {
    implementation project(':limo-core')

    testImplementation testFixtures(project(':limo-core'))
}
